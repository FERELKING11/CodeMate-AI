# CodeMate AI Backend Configuration

# Server configuration
HOST=0.0.0.0
PORT=8080
ENV=development

# Workspace root directory (default: current directory)
WORKSPACE_ROOT=.

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
